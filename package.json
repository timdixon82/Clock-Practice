{
  "name": "clock-practice",
  "version": "0.1.0",
  "description": "Accessible clock-reading practice tool.",
  "private": true,
  "type": "module",
  "scripts": {
    "lint:html": "html-validate index.html",
    "lint:css": "stylelint styles/styles.css",
    "lint:js": "eslint scripts/clock-practice.js scripts/clock-logic.js tests",
    "lint": "npm run lint:html && npm run lint:css && npm run lint:js",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "html-validate": "^11.5.6",
    "stylelint": "^17.14.0",
    "stylelint-config-standard": "^40.0.0",
    "vitest": "^4.1.10"
  }
}
